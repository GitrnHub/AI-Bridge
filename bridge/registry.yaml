bridge_version: 2
# Index only; workspace state is authoritative.
workspaces:
  - id: ofa
    path: workspaces/ofa
    state: workspaces/ofa/.bridge/state.yaml
    branch: OFA
    status: ready_for_codex
    next_actor: codex
